import CustomerOffers from '@/components/dashboardComponents/customerDashboard/CustomerOffers';
import React from 'react';

const page = () => {
    return (
        <div>
            <CustomerOffers />
        </div>
    );
};

export default page;

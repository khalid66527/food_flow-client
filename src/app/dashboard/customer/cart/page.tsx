import CustomerCart from '@/components/dashboardComponents/customerDashboard/CustomerCart';
import React from 'react';

const page = () => {
    return (
        <div>
            <CustomerCart />
        </div>
    );
};

export default page;

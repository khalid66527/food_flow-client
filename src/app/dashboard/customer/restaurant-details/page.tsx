import RestaurantDetails from '@/components/dashboardComponents/customerDashboard/RestaurantDetails';
import React from 'react';

const page = () => {
    return (
        <div>
            <RestaurantDetails />
        </div>
    );
};

export default page;

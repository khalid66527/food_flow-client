import RiderHistory from '@/components/dashboardComponents/riderDashboard/RiderHistory';
import React from 'react';

const page = () => {
    return (
        <div>
            <RiderHistory />
        </div>
    );
};

export default page;
